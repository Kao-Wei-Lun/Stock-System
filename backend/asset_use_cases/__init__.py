"""Application-layer use cases for the personal asset subsystem.

The HTTP router keeps backward-compatible facades, while this package owns
commands, queries, provider hydration helpers, imports, and reconciliation.
"""
